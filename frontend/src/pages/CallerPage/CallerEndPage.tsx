import CallerEndTemplate from '@features/caller/component/CallerEndTemplate.tsx';

const CallerEndPage = () => {
  return (
    <CallerEndTemplate>
    </CallerEndTemplate>
  );
};

export default CallerEndPage;